<!DOCTYPE html>  
<html lang="en">  
<head>  
<meta charset="UTF-8" />  
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>  
<title>For Khea 💖</title>  
  
<style>  
  body {  
    margin: 0;  
    height: 100vh;  
    background: #fff0f5;  
    display: flex;  
    justify-content: center;  
    align-items: center;  
    font-family: system-ui, -apple-system, sans-serif;  
    overflow: hidden; 
  }  
  
  .card {  
    background: white;  
    width: 90%;  
    max-width: 400px;  
    padding: 40px 20px;  
    border-radius: 25px;  
    box-shadow: 0 15px 35px rgba(0,0,0,0.15);  
    text-align: center;  
    position: relative;
  }  
  
  h1 { font-size: 24px; color: #ff4d6d; margin-bottom: 25px; }  
  
  .buttons {  
    position: relative;  
    height: 300px; /* Space for the button to move */
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }  
  
  button {  
    border: none;  
    border-radius: 30px;  
    padding: 12px 25px;  
    font-size: 18px;  
    font-weight: bold;
    cursor: pointer;  
    transition: all 0.3s ease; /* Smoother transitions */
  }  
  
  #yes { background: #ff4d6d; color: white; z-index: 10; }  
  #no { position: absolute; background: #ccc; color: #333; top: 180px; }

  #gif-container { display: none; }
  .celebration-gif { width: 80%; border-radius: 15px; margin-top: 15px; }
</style>  
</head>  
  
<body>  
  <div class="card">  
    <div id="question-content">
      <h1>Khea, will you be my Valentine? 💘</h1>  
      <div class="buttons">  
        <button id="yes">Yes 💖</button>  
        <button id="no">No 😅</button>  
      </div>  
    </div>

    <div id="gif-container">
      <h1>YAY!!! 💕🥰 I KNEW IT!</h1>
      <img src="https://media.tenor.com/S6pEAnY_S_4AAAAM/yuli-kwon-yuli.gif" alt="Happy Kid" class="celebration-gif">
    </div>
  </div>  
  
<script>  
  let yesScale = 1;  
  let noScale = 1;  
  let noClicks = 0;
  const maxNoClicks = 6; 
  
  const yesBtn = document.getElementById("yes");  
  const noBtn = document.getElementById("no");  
  const questionContent = document.getElementById("question-content");
  const gifContainer = document.getElementById("gif-container");
  
  yesBtn.onclick = () => {  
    questionContent.style.display = "none"; 
    gifContainer.style.display = "block";  
  };  
  
  noBtn.onclick = () => {  
    noClicks++;
    if (noClicks >= maxNoClicks) {
      noBtn.style.display = "none"; 
    } else {
      // Move No button randomly
      const x = Math.random() * 160 - 80;  
      const y = Math.random() * 120 + 40;  
      noBtn.style.left = `calc(50% + ${x}px)`;  
      noBtn.style.top = `${y}px`;  
      
      // Shrink No button
      noScale -= 0.12;  
      noBtn.style.transform = `translateX(-50%) scale(${noScale})`;  
      
      // GROW YES SLOWER: Reduced from 0.5 to 0.15
      yesScale += 0.15; 
      yesBtn.style.transform = `scale(${yesScale})`; 
    }
  };  
</script>  
</body>  
</html>